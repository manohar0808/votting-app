apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "result-helm.fullname" . }}-test-connection"
  labels:
    {{- include "result-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "result-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
